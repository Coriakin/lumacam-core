name: CI

on:
  push:
  pull_request:

jobs:
  swift-tests:
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v4

      - uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: "16"

      - name: Test (all targets)
        run: swift test
